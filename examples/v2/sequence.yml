version: '2'
tasks:
  test:
    description: running steps
    do: sequence
    steps:
      - description: running tests
        do: shell
        command: npm test
      - description: printing done
        do: shell
        command: echo done
