
basic:
  cmd: docker build
  tags:
    - image: usher
      tag: test

custom:
  cmd: do thing

sequence:
  - cmd: docker build
    tags:
      - image: usher
  - cmd: do thing

with_settings:
  cmd: docker build
  retry:
    attempts: 5
  ignore_errors:
    - 5
    - 6

bogus:
  blah: meh

null:
