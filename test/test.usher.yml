
basic:
  cmd: docker build
  tags:
    - usher

custom:
  cmd: do thing

sequence:
  - cmd: docker build
    tags:
      - usher
  - cmd: do thing

retry:
  cmd: docker build
  retry:
    attempts: 5

bogus:
  blah: meh

null:
